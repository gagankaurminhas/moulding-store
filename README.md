# Moulding Store — Stable Light Luxury

Leaflet + OpenStreetMap + Photon (no API key), Alberta-only address search.

After replacing the GitHub Pages files, open `/reset.html` once to remove any old service worker/cache. Your local deliveries remain in localStorage.
